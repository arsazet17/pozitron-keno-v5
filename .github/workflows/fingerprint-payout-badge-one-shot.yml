name: KENO · LEGACY ONE-SHOT DISABLED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Старое одноразовое исправление отключено
        run: echo "Исправление уже встроено; workflow ничего не изменяет"
